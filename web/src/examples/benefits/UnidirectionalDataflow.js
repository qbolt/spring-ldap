/*
Unidirectional Dataflow
-----------------------------------
State => UI
The UI is a function of the state.
-----------------------------------

The website just collects a big ball of state from the user, and as the state
updates, the UI will update accordingly.

It doesn't get much more complicated than that.
*/
